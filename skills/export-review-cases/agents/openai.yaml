interface:
  display_name: "Find Review Cases"
  short_description: "Where an AI reviewer did well, and where it didn't"
  default_prompt: "Use $export-review-cases to find concrete cases where the AI code reviewer on this repository caught a real bug and where it missed one, with evidence for each."
