interface:
  display_name: "Resolve Comments"
  short_description: "Resolve PR comments and assess Zeus handoffs"
  default_prompt: "Use $resolve-comments to process every unresolved PR review comment and actionable top-level PR comment for this PR URL or number, and assess each Zeus inline agent prompt as a standalone coding-agent handoff."
